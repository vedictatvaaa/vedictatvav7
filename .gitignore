node_modules
dist
.DS_Store
server/public
vite.config.ts.*
*.tar.gz

# Environment and secrets
.env
.env.*

# Cache and local directories
.cache/
.local/
.upm/

# Database backups
*.sql
*.sql.gz
backups/
# Local-only directories (not in git, sync'd separately)
downloads/
attached_assets/
artifacts/

# Local-only directories (kept on VPS, not in git)
downloads/
attached_assets/
artifacts/
# Runtime upload subdirs — only seed files we explicitly add live in uploads/
uploads/invoices/
uploads/kundli-pdfs/
uploads/labels/
uploads/mantra-audio/
uploads/sacred-audio/
